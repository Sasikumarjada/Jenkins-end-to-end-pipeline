pipeline {
    agent {
        node {
            label 'test'
        }
    }
    environment {
        SCANNER_HOME ="mysonar"
    }
    tools {
        maven "my maven"
    }
    stages {
        stage('code') {
            steps {
                git branch: 'feature', url: 'https://github.com/Sasikumarjada/demorepo.git'
            }
        }
          stage('build') {
            steps {
                sh "mvn clean package"
            }
        }
          stage('CQA') {
            steps {
                sh '''
                    mvn sonar:sonar \
                        -Dsonar.projectKey=sonarproject \
                        -Dsonar.host.url=http://34.230.83.28:9000 \
                        -Dsonar.login=a6dec311046d690ef775b84bd1614cb353edca21
                '''
            }
        }
         stage('Artifact') {
            steps {
                nexusArtifactUploader artifacts: [[artifactId: 'myweb', classifier: '', file: 'target/myweb-8.6.9.war', type: 'war']], credentialsId: 'nexux', groupId: 'in.javahome', nexusUrl: '34.229.233.191:8081/', nexusVersion: 'nexus3', protocol: 'http', repository: 'myrepo', version: '8.6.9'
            }
        }
           stage('deploy') {
            steps {
                deploy adapters: [tomcat9(alternativeDeploymentContext: '', credentialsId: 'tomcat', path: '', url: 'http://54.92.175.248:8080/')], contextPath: 'e-commerce', war: 'target/*.war'
            }
        }
    }
}
